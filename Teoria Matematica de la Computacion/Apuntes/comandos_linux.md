### Directorios principales en linux
+ /bin
+ /boot
+ /dev
+ /etc
+ /lib
+ /mnt
+ /root
+ /home

> Terminal vs Interprete de comandos  
> La terminal es la interfáz gráfica mientras que el interprete de comandos es el componente  
> central que recibe, interpreta y ejecuta los comandos `Bash, ZSH, PowerShell, CMD`

